'use strict';

describe('Service: UserSession', function () {
/*
 * Stubbed out the whole damn thing. Don't even bother instantiating this one. It uses
 * $route and I have no idea how to stub that in.
 */
  // load the service's module
//  beforeEach(module('pulsarClientApp'));

  // instantiate service
  // var UserSession;
  // beforeEach(inject(function (_UserSession_) {
  //   UserSession = _UserSession_;
  // }));

//   it('should do something', function () {
//     expect(!!UserSession).toBe(true);
//   });

});
